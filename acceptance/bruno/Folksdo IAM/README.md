# Folksdo IAM™ Acceptance

Prepare the deterministic local certification universe from the IAM repository root:

```bash
pnpm certification:local
```

Keep that process running. Open this collection in Bruno, select `local`, and run `01 - IAM`.

The suite is self-contained and does not depend on another Bruno collection or execution order.
