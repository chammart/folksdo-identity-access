info:
  name: IAM — Missing Authentication Rejected
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/membership/current"
  headers:
    - name: Accept
      value: "application/json"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Protected IAM boundary rejects missing authentication", () => {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
